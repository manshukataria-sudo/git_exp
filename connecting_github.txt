To check if the repo is connected online or not use the command
git remote show origin


github uses the master as the main branch
use the command 
git branch -M main


origin/main represents the code stored on remote repo till the branch


Use the command to push the branch on remote github
git push -u origin <branchName>